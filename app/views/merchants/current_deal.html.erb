<%= render 'nav' %>
<div class="row-fluid">
	<div class="span3">
	<%= render 'shared/merchant_badge', merchant: @merchant %>
	</div>
	<div class="span6">
		<% if @merchant.current_deal? %>
			<%= render "shared/deal_view", deal: @merchant.current_deal %>
		<% else %>
			<h2>No Referral Program</h2>
			<h5>Incentivize your raving fans to rave about you.</h5>
			<a class="btn btn-success btn-large">Start Raving!</a>
		<% end %>
	</div>
</div>